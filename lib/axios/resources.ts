export enum ApiEndpointResource {
  TOURNAMENTS = "tournaments",
}

export enum UserResource {
  USERS = "users",
  USERS_ME = "users/me",
}

export enum TournamentResource {
  TOURNAMENTS = "tournaments",
}
